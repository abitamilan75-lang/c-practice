#include <stdio.h>

int main() {
   int a,b,c;
   printf("enter a character : ");
   scanf("%d%d%d",&a,&b,&c);
    
     
   if (a>b) 
   {
      printf(" %d  is greater than") ;
   }
    else if (b>c) {
     printf(" %d  is greater than");
    }
    else (c>a);
    {
        printf("%d  is greater than");
    }
       
   

    return 0;
}